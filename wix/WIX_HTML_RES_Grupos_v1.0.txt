<!doctype html>
<html lang="es">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SCaD Residencial · Grupos</title>
<style>
:root{--bg:#f4f7fb;--card:#fff;--ink:#172033;--muted:#6b7280;--line:#dfe5ee;--accent:#1f5eff;--ok:#147d55;--danger:#b42318;--soft:#eef3ff;--radius:16px;--shadow:0 10px 30px rgba(20,34,66,.08)}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,textarea{font:inherit}.wrap{max-width:1100px;margin:auto;padding:18px}.top{display:flex;gap:14px;justify-content:space-between;align-items:flex-start;margin-bottom:16px}h1{font-size:23px;margin:0 0 4px}.sub{margin:0;color:var(--muted);font-size:13px}.pill{background:var(--soft);padding:9px 12px;border-radius:999px;font-size:12px;font-weight:700;white-space:nowrap}.grid{display:grid;grid-template-columns:1fr 1.2fr;gap:16px}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden}.cardHead{padding:16px 18px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;gap:10px}.cardBody{padding:16px 18px}.btn{border:0;border-radius:11px;padding:10px 13px;cursor:pointer;font-weight:700}.btnPrimary{background:var(--accent);color:#fff}.btnGhost{background:#f3f5f9;color:var(--ink)}.btnDanger{background:#fff0ee;color:var(--danger)}.btn:disabled{opacity:.45;cursor:not-allowed}.group{border:1px solid var(--line);border-radius:13px;padding:13px;margin-bottom:10px;cursor:pointer;background:#fff}.group.activeSel{border-color:var(--accent);box-shadow:0 0 0 2px rgba(31,94,255,.08)}.row{display:flex;align-items:center;gap:8px}.spread{justify-content:space-between}.gName{font-weight:800}.meta{font-size:12px;color:var(--muted);margin-top:5px}.dot{width:8px;height:8px;border-radius:50%;background:#a8b0bd}.dot.on{background:var(--ok)}.field{margin-bottom:12px}.field label{display:block;font-size:12px;font-weight:800;margin-bottom:6px}.input{width:100%;border:1px solid var(--line);border-radius:11px;padding:10px 11px;outline:none;background:#fff}.input:focus{border-color:var(--accent);box-shadow:0 0 0 3px rgba(31,94,255,.08)}textarea.input{min-height:78px;resize:vertical}.actions{display:flex;gap:8px;flex-wrap:wrap}.sep{height:1px;background:var(--line);margin:16px 0}.searchResults,.members{display:grid;gap:8px}.person{border:1px solid var(--line);border-radius:12px;padding:10px 11px;display:flex;justify-content:space-between;gap:10px;align-items:center}.personName{font-weight:750;font-size:14px}.personMeta{font-size:11px;color:var(--muted);margin-top:3px}.smallBtn{border:0;border-radius:9px;padding:7px 9px;cursor:pointer;font-size:12px;font-weight:800;background:var(--soft);color:#2447a5}.empty{padding:18px;text-align:center;color:var(--muted);font-size:13px;border:1px dashed var(--line);border-radius:12px}.toast{position:fixed;left:50%;bottom:18px;transform:translateX(-50%);background:#172033;color:white;padding:10px 14px;border-radius:10px;font-size:13px;opacity:0;pointer-events:none;transition:.2s}.toast.show{opacity:1}@media(max-width:760px){.wrap{padding:12px}.grid{grid-template-columns:1fr}.top{align-items:center}h1{font-size:20px}.cardBody,.cardHead{padding:14px}}
</style></head>
<body><div class="wrap"><div class="top"><div><h1>Gestión de grupos</h1><p class="sub">Audiencias reutilizables para Documentación y futuros módulos.</p></div><div class="pill" id="quota">0 / 5 activos</div></div><div class="grid"><section class="card"><div class="cardHead"><strong>Grupos</strong><button class="btn btnPrimary" id="newBtn">+ Nuevo grupo</button></div><div class="cardBody"><div id="groups"></div></div></section><section class="card"><div class="cardHead"><strong id="editorTitle">Selecciona un grupo</strong><span class="meta" id="groupId"></span></div><div class="cardBody"><div id="editorEmpty" class="empty">Selecciona un grupo o crea uno nuevo.</div><div id="editor" hidden><div class="field"><label>Nombre del grupo</label><input class="input" id="name" maxlength="80"></div><div class="field"><label>Descripción</label><textarea class="input" id="desc" maxlength="300"></textarea></div><div class="actions"><button class="btn btnPrimary" id="saveBtn">Guardar</button><button class="btn btnGhost" id="toggleBtn">Desactivar</button></div><div class="sep"></div><strong>Integrantes</strong><div class="meta" style="margin:4px 0 10px">Busca por nombre, vivienda/residencia o correo.</div><input class="input" id="search" placeholder="Buscar usuario…" autocomplete="off"><div id="searchResults" class="searchResults" style="margin-top:9px"></div><div class="sep"></div><div class="row spread"><strong>Usuarios del grupo</strong><span class="meta" id="memberCount">0 integrantes</span></div><div id="members" class="members" style="margin-top:9px"></div></div></div></section></div></div><div class="toast" id="toast"></div>
<script>
const state={groups:[],limit:5,active:0,selected:null,isNew:false,members:[],search:[]};const $=id=>document.getElementById(id);function send(action,data={}){window.parent.postMessage({action,...data},'*')}function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function toast(t){const el=$('toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)}function errText(code){const m={NO_AUTENTICADO:'Debes iniciar sesión.',NO_AUTORIZADO:'Tu usuario no tiene permiso de Administrador.',NOMBRE_REQUERIDO:'Indica el nombre del grupo.',LIMITE_5_GRUPOS_ACTIVOS:'Ya existen 5 grupos activos. Desactiva uno para habilitar otro.',NOMBRE_GRUPO_DUPLICADO:'Ya existe un grupo con ese nombre.',GRUPO_NO_ENCONTRADO:'El grupo ya no está disponible.',USUARIO_NO_DISPONIBLE:'El usuario no está activo o ya no está disponible.'};return m[code]||'No fue posible completar la operación.'}function renderGroups(){$('quota').textContent=`${state.active} / ${state.limit} activos`;$('newBtn').disabled=state.active>=state.limit;const box=$('groups');if(!state.groups.length){box.innerHTML='<div class="empty">Todavía no hay grupos.</div>';return}box.innerHTML=state.groups.map(g=>`<div class="group ${state.selected?.grupoId===g.grupoId?'activeSel':''}" data-id="${esc(g.grupoId)}"><div class="row spread"><span class="gName">${esc(g.nombre)}</span><span class="row meta"><span class="dot ${g.activo?'on':''}"></span>${g.activo?'Activo':'Inactivo'}</span></div><div class="meta">${g.integrantes||0} integrante${g.integrantes===1?'':'s'}${g.descripcion?' · '+esc(g.descripcion):''}</div></div>`).join('');[...box.querySelectorAll('.group')].forEach(el=>el.onclick=()=>selectGroup(el.dataset.id))}function renderEditor(){const show=!!state.selected||state.isNew;$('editorEmpty').hidden=show;$('editor').hidden=!show;if(!show)return;const g=state.selected||{nombre:'',descripcion:'',activo:true,grupoId:''};$('editorTitle').textContent=state.isNew?'Nuevo grupo':g.nombre;$('groupId').textContent=state.isNew?'':g.grupoId;$('name').value=g.nombre||'';$('desc').value=g.descripcion||'';$('toggleBtn').hidden=state.isNew;$('toggleBtn').textContent=g.activo?'Desactivar':'Activar';$('toggleBtn').className='btn '+(g.activo?'btnDanger':'btnGhost');$('search').disabled=state.isNew;if(state.isNew){$('searchResults').innerHTML='<div class="empty">Guarda primero el grupo para agregar integrantes.</div>';$('members').innerHTML='';$('memberCount').textContent='0 integrantes'}}function renderMembers(){$('memberCount').textContent=`${state.members.length} integrante${state.members.length===1?'':'s'}`;$('members').innerHTML=state.members.length?state.members.map(u=>personHtml(u,'remove')).join(''):'<div class="empty">Este grupo no tiene integrantes.</div>';[...$('members').querySelectorAll('[data-remove]')].forEach(b=>b.onclick=()=>send('REMOVE_MEMBER',{grupoId:state.selected.grupoId,memberId:b.dataset.remove}))}function renderSearch(){const memberSet=new Set(state.members.map(x=>x.memberId));const users=state.search.filter(x=>!memberSet.has(x.memberId));$('searchResults').innerHTML=users.map(u=>personHtml(u,'add')).join('');[...$('searchResults').querySelectorAll('[data-add]')].forEach(b=>b.onclick=()=>send('ADD_MEMBER',{grupoId:state.selected.grupoId,memberId:b.dataset.add}))}function personHtml(u,mode){const extra=[u.vivienda,u.email,u.rolResidencial].filter(Boolean).map(esc).join(' · ');return `<div class="person"><div><div class="personName">${esc(u.nombreCompleto||'Usuario')}</div><div class="personMeta">${extra}</div></div><button class="smallBtn" data-${mode}="${esc(u.memberId)}">${mode==='add'?'+ Agregar':'Quitar'}</button></div>`}function selectGroup(id){state.isNew=false;state.selected=state.groups.find(g=>g.grupoId===id)||null;state.search=[];renderGroups();renderEditor();$('searchResults').innerHTML='';send('LOAD_MEMBERS',{grupoId:id})}$('newBtn').onclick=()=>{if(state.active>=state.limit)return;state.isNew=true;state.selected=null;state.members=[];state.search=[];renderGroups();renderEditor()};$('saveBtn').onclick=()=>{const data={nombre:$('name').value.trim(),descripcion:$('desc').value.trim()};if(!data.nombre){toast('Indica el nombre del grupo.');return}if(state.isNew)send('CREATE_GROUP',{data});else send('UPDATE_GROUP',{data:{grupoId:state.selected.grupoId,...data}})};$('toggleBtn').onclick=()=>{if(!state.selected)return;send('SET_GROUP_ACTIVE',{grupoId:state.selected.grupoId,activo:!state.selected.activo})};let timer;$('search').oninput=()=>{clearTimeout(timer);const texto=$('search').value.trim();if(texto.length<2){state.search=[];renderSearch();return}timer=setTimeout(()=>send('SEARCH_USERS',{texto}),260)};window.onmessage=(event)=>{const m=event.data||{};if(m.type==='STATE'){state.groups=m.data?.grupos||[];state.limit=m.data?.limite||5;state.active=m.data?.activos||0;if(state.isNew&&m.toast){state.isNew=false;state.selected=state.groups[state.groups.length-1]||null}else if(state.selected){state.selected=state.groups.find(g=>g.grupoId===state.selected.grupoId)||null}renderGroups();renderEditor();if(m.toast)toast(m.toast);if(state.selected)send('LOAD_MEMBERS',{grupoId:state.selected.grupoId})}if(m.type==='MEMBERS'){state.members=m.data?.integrantes||[];renderMembers();renderSearch();if(m.toast)toast(m.toast)}if(m.type==='USER_RESULTS'){state.search=m.data?.usuarios||[];renderSearch()}if(m.type==='ERROR')toast(errText(m.code))};send('INIT');
</script></body></html>